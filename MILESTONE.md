# PagePure 版本台账

## 当前版本 0.7.5 搜索后悬浮球恢复

修复页面局部刷新清空 body 内容或替换 body 后悬浮球不再出现的问题：自动重新挂载同一个入口，保留拖动位置，关闭旧页面设置面板。只观察 body 和根节点的直接子节点变化。184 项测试通过，百度搜索后悬浮球恢复已由用户测试通过。2026-09-21 以 `PagePure-0.7.5.zip` 提交 Edge 商店审核（0.6.32 同日已过审上架）。

## 0.7.4 自定义 AI 需求执行修复

修正 AI 提示词与自由文本设置不一致：完整净化需求作为判断依据，支持只保留指定内容的意图，必要阅读操作与手动规则继续优先。嵌入区域补充标题和去除查询参数的来源信息，旧 AI 缓存失效。183 项测试通过；已在 Edge 的 CSDN 页面确认原版仅一处 AI 隐藏，修复后的线上 Jev 效果待重新加载扩展验收。本版为本地开发版本，尚未发布。

## 0.7.3 选区编辑优化与可拖动悬浮球

新增编辑时收起已隐藏区域、隐藏诊断日志，以及同栏目同构区域的确认批量隐藏与恢复；改进拆分后的区域定位和旧恢复规则冲突处理。规则管理页沿用蓝色主题细化视觉。左下角入口改为可拖动的圆形净化印章，支持现有 12 种语言与实时文字更新，面板自动保持在视口内。自动 AI 判断改为规则放行后的异步补充：明确隐藏或保留的区域优先，未覆盖区域显示后再交给 Jev；返回时复查规则与内容，过期结果丢弃。默认需求为“屏蔽：商业广告、付费推广、赞助内容、诱导下载或购买的营销模块”，支持编辑，旧自定义需求保留。自定义需求允许自由填写并按网站保存，修复升级后旧语言缓存残留。完整测试 181 项通过；选区编辑已由用户验收，悬浮球待用户验收。本版为本地开发版本，尚未发布。

## 0.7.1 · 国际化修复：网页内工具条语言生效

2026-09-21：面向全球用户完成国际化。语言矩阵：简体中文（默认/回退）、繁體中文、English、日本語、한국어、Español、Português (Brasil)、Русский、Deutsch、Français、Italiano、Türkçe，共 12 种 × 196 键。原 0.6.33/0.6.34 未单独交付，合并为 0.7.0。2026-09-21 晚：用户实测发现选日语后网页内工具条仍为中文。第一版修复尝试把全部语言文件登记进 web_accessible_resources，但内容脚本对扩展文件的 fetch 在部分浏览器构建中仍会被拦截。最终方案（第三版，已真机定位根因）：本扩展的 chrome.storage.local 被 background 以 setAccessLevel(TRUSTED_CONTEXTS) 刻意锁定为仅受信环境可读（其中存有 API Key，0.6.31 起的安全约束，不得放开），内容脚本根本读不到 storage——此前两版让内容脚本读 storage/fetch 语言文件均因此失败。现改为与扩展原有架构一致的通道：设置页（受信）保存语言时把完整消息表随 uiLocale 写入 storage（uiMessages）；后台新增 i18nGet 消息（先等待自身 i18n 就绪以自动补写旧格式保存，再返回语言表）；内容脚本 i18n 启动与实时切换均通过 chrome.runtime.sendMessage 向后台获取，语言变更由后台 storage.onChanged 广播 localeChanged 通知已打开网页；扩展页/后台仍直接读 storage。语言文件不进 WAR。新增「内容脚本经消息获取语言表（禁 fetch/storage）」「扩展页直读与旧格式自愈」「localeChanged 实时切换」「后台 i18nGet 处理器」等测试，146/146 通过。基础国际化内容见下。新增 `_locales/zh_CN` 与 `_locales/en` 消息表（默认语言 zh_CN），manifest 名称/描述改用 `__MSG_extName__`/`__MSG_extDescription__`，Edge/Chrome 等浏览器界面语言为英文时自动显示英文文案，其余语言回退中文；中文文案逐字未变。新增 `extension/i18n.js` 运行时（内容脚本清单首位注入，扩展页在页面脚本前加载，后台模块以副作用导入），页面静态文案用 `data-i18n*` 属性标注并在加载时替换。发往 TypeSafe 的 AI 提示词与站点阅读需求默认值保持中文原文，不属于界面文案，翻译会改变模型行为。规则管理页“版本冲突”判定改为依赖 `code='revision'` 错误标记，跨语言可靠。demo 演示页支持 `?lang=<locale>` 预览各语言界面。规则管理页新增设置视图（侧栏「设置」）：界面语言下拉列出全部 12 种语言（每种以母语自称，如 日本語、Русский），选择后随完整消息表一起保存到 chrome.storage.local，本页立即刷新生效，已打开网页的工具条经后台广播实时生效，未选择时跟随浏览器界面语言。i18n.test.mjs 守护全矩阵：任一语言缺键、多键、空值或 {n} 占位符与 zh_CN 不一致都会测试失败；另含 HTML 静态文案防漂移、脚本无硬编码中文、语言覆盖运行时单测；规则管理页设置视图测试在内，142/142 项测试通过（0.7.0 阶段）。浏览器实测中文/英文/日文/俄文工具条与净化章（浄化/Clean/Чисто）排版正常，语言设置切换含自动刷新往返正常。交付 PagePure-0.7.1.zip；Edge 商店当前为 0.6.32 审核中，本版待 0.6.32 过审后另行安排提交。

## 0.6.32 · 移除类别净化

2026-09-21：按用户要求移除隐藏同类、调整分类及普通隐藏的类别回退；禁用旧类别规则读取/迁移及分类消息入口。普通浏览仅匹配选择器，移除类别描述、快照和自动拆分开销，显式智能拆分及保存边界保留。旧数据读取不改写，管理页不计停用类别规则；保存/撤销不使其重新生效。133项现行测试通过（替换已移除功能测试）；浏览器验证菜单、区域隐藏保存及刷新恢复，截图 output/playwright/manual-only-toolbar.png。交付 PagePure-0.6.32.zip，未提交商店。

## 0.6.32 阶段收束与验收（2026-09-21）

用户在本会话明确反馈“我测试ok”，确认0.6.32功能通过实测。本轮只对齐 README、版本台账和发布记录，不改变代码、版本或交付包。

- 收束基线：`ae35ba930a937021de467370bf8926d07a4d37c9`（0.6.31，移除类别功能之前的已记录提交）；代码成果 HEAD：`c491ed555468f1dece2898af1489e516a154031c`。基线为 HEAD 祖先，delta 为1次提交、15个文件，覆盖实现、测试、演示、版本配置和文档。
- Preflight 完整工作区状态：tracked/staged/unstaged/untracked 均为0；阶段改动归属于本会话 Agent。当前事实源为 README 当前使用说明、本节和 STORE_PUBLISH 当前发布状态；下方旧版本记录及0.6.24验收表为 history。
- 验证绑定：2026-09-21 13:41（Asia/Shanghai），在上述干净代码 HEAD 执行 `npm test`，133/133通过。此次后续仅修改文档，运行代码、测试和依赖不变，因此该证据继续适用。`git diff --check` 在文档提交前核验。先前浏览器检查和安装包校验属于该版本交付时的观察，本轮未重新读取构建产物。
- 当前契约：区域隐藏/恢复、仅本页例外、扩大/缩小、显式智能拆分及边界保存保留；类别入口、类别回退和分类消息已移除，旧类别规则不执行、不迁移、不计入管理页有效数量。普通AI自动判断作为独立高级设置保留，不能将“移除类别净化”解释为移除所有AI能力。
- 交付状态：本地0.6.32已验收；Edge商店最后已记录提交仍为0.6.24，本次不操作商店。后续更新使用新版区域操作文案，不复用旧类别功能宣传。
- Git推送：用户本轮已明确授权普通push；Preflight未配置远端，已请求用户提供目标地址。是否推送成功以实际Git回执为准，不沿用旧阶段“未授权”的描述。
- 后续仅按实际反馈处理网站结构变化；旧类别规则隐藏的内容如需继续隐藏，使用区域选择重新保存。不要恢复类别推断或以重装作为维护要求。

## GitHub 公开分发（2026-09-21）

经用户明确授权创建公开仓库并推送：<https://github.com/mhgd3250905/PagePure>，远端 origin，分支 master。原本“无远端待地址”的阻塞已解除。

- 首次推送成果：`a6289d7`，包含代码与小白安装指南；版本历史迁至 `docs/VERSION_HISTORY.md`，README 保留下载安装、基本使用及更新步骤。
- 已发布 GitHub Release `v0.6.32`：<https://github.com/mhgd3250905/PagePure/releases/tag/v0.6.32>，不是草稿或预发布。资产 `PagePure-0.6.32.zip` 共116037字节，包内文件与当前 extension 源码一致。
- 本地与 GitHub 返回的 SHA-256 一致：`cfba9af93085f712204fb236e7ca4bee4936c3c100292ddea1fe149bd2e4e25f`。
- 本轮未更改运行代码与测试，继续适用 c491ed5 的133项测试证据；仅完善文档和公开分发记录。GitHub发布不代表Edge商店审核通过，商店状态仍按历史已确认记录。

## 历史版本记录

以下各版本的验收表、收束处置、验证与剩余事项描述均是当时状态；当前结论以本页顶部0.6.32及本次收束记录为准。

## 0.6.31 · 星愿浏览器启动兼容

2026-09-21：在本机星愿9.0.1000.2404独立配置中复现 storage.local.setAccessLevel 缺失，后台创建消息监听器前同步抛错。可选API按能力调用，支持的浏览器继续设置 TRUSTED_CONTEXTS；旧内核不能提供该额外存储访问隔离。真实浏览器验证：设置正常读取、按钮启用、未配置密钥、网页内进入选择模式成功。163项测试通过，交付 PagePure-0.6.31.zip，未提交商店。

## 0.6.30 · 操作台连接恢复

2026-09-21：用户报告新安装操作台永久读取。修复初始化缺少超时和重试的问题，8秒超时后显示重新连接；页面状态串行轮询，迟到的初始化响应不覆盖重试结果。新增3项回归测试，162项测试通过。交付 PagePure-0.6.30.zip，未提交商店；实际浏览器连接阻塞根因尚未确认，不能宣称已复现并修复环境原因。

## 0.6.29 · 修复自动快照定位热点

2026-09-21：基于用户 Trace-20260921T104734.json.gz 定位 MutationObserver 回调累计2362ms/185次、最长237ms。自动快照最多24次候选定位查询，成功后再核验一次，不执行手动定位的 :has/位置回退，不读取 innerText；缓存失败定位，结构和身份变化时重试。新增查询预算、手动回退兼容及失败缓存失效回归测试；159项测试通过。Linkedom模拟8张重复卡片：1032→192次查询，46.14→5.17ms（单次模拟，非浏览器端到端对比）。交付 PagePure-0.6.29.zip，尚未提交商店；真实Edge/Chrome差距仍待用户复验。

## 0.6.28 · 减少净化扫描开销

2026-09-21：针对首屏内容慢、滚动点击卡顿，去除同批变化的延迟重复扫描、手动规则不必要分块、已知内容块后代的全量包含关系检查；编辑滚动合并到每帧一次。157 项测试通过。模拟长页 60 块、每块 60 段，浏览器内五轮均值：14.44ms → 1.64ms，包含检查 221430 → 0，候选数保持60；不代表真实 Edge 端到端测速。交付 `PagePure-0.6.28.zip`，待用户相同页面复验；不清理已有规则/快照，不调整AI等待和首屏显示策略。

## 0.6.27 · 净化规则管理

2026-09-21：新增扩展独立管理页与操作台入口，集中展示地址范围和规则数量；支持搜索、单选、多选、批量清理确认。仅管理页可跨站管理，按实际作用范围拆出页面例外；清理使相关旧撤销记录失效并通知已打开网页，退出未保存的编辑草稿。保留用户密钥和站点设置。交付 `PagePure-0.6.27.zip`，尚未提交商店。验证：`npm test` 154/154；桌面与390px窄屏使用模拟数据验证搜索、多选、确认与清理反馈；包内 25 个文件逐 SHA-256 与源码一致。未读取或清理用户真实浏览器规则。

## 0.6.26 · 智能拆分入口

智能拆分恢复默认可见，保留原名称；未配置 AI 时点击提示，禁止发送拆分请求。交付 `PagePure-0.6.26.zip`，尚未提交商店。`npm test` 145/145 通过，包内 21 个文件与源码逐文件一致。

## 0.6.25 · 紧凑分组操作条

2026-09-20：用户确认新版布局并要求推进更新。320px 点选面板按作用范围成对展示隐藏/恢复，底部预览与保存，明确按钮边界与反馈，隐藏高级规则清单和冗长说明。面板尺寸改变时重新定位以保持在视口内。

交付包 `PagePure-0.6.25.zip`；使用说明以 README 为准。0.6.24 已提交商店审核的历史记录保留，0.6.25 尚未提交商店。验证：`npm test` 144/144 通过；浏览器检查折叠/展开布局和 375×300 视口边界；安装包 21 个文件逐 SHA-256 与 `extension/` 一致。以下 0.6.24 内容为历史基线。

## 0.6.24 · 第一个可用版本台账

日期：2026-09-20。用户确认当前版本为第一个可用版本。本阶段冻结当前 `extension/`、测试、依赖配置、演示页面和项目文档，不另升版本。

## 验收与事实

| 项目 | 当前状态 | 依据 |
| --- | --- | --- |
| 手动选择、隐藏、恢复、预览、保存、取消 | 已实现 | preview/manual/backend 测试 |
| 当前页面隐藏与恢复例外 | 已实现；优先于共享规则 | preview/manual/backend 测试 |
| 快照快速恢复与父区域状态继承 | 已实现；普通子内容不重复等待识别，明确子规则仍生效 | layout-snapshot/preview/startup 测试；用户反馈正文迅速显示 |
| 滚动时保持隐藏 | 已实现差量更新与隐藏区域收集 | preview/blocks 测试 |
| 左下操作台美化 | 保留 0.6.22 改动 | console 测试及之前的独立页面渲染检查 |
| 块点选展示 | 已恢复原工具条；右上说明框取消独立展示 | 0.6.23 修正与 preview 测试 |
| 长区域净化盖章 | 定位到可见部分中心 | 0.6.24 新增长区域滚动测试 |
| 用户阶段验收 | 用户确认“第一个可用版本” | 本次阶段收束请求 |

当前产品契约和使用方法见 README；实现以 extension 为准。旧压缩包保留为历史产物，不覆盖或清理。

## 本次收束处置

- README 增加可用基线及安装入口，修正旧分发包、82 项测试、默认页面类型范围、红色选区及旧设置名称。
- 保留旧版历史记录，明确 0.6.22 点选视觉方案被 0.6.23 替代、拆分持久化在 0.6.13 实现。
- 本台账为当前阶段状态唯一入口；没有变更扩展实现、规则或用户存储。
- `.gitignore` 排除依赖、压缩包、浏览器检查截图和本地密钥配置；这些文件留在磁盘，不纳入源码检查点。
- 首次 Git 检查按 Windows CRLF 识别行尾，修正四个文件的行尾空格或末尾空行；仅格式整理，重新生成同版本分发包并复验测试。

## 验证与交付边界

- Cockpit closeout 基线：`cd47369d548d024fa529976271901b3e4ad210ce`（master 首次提交，用户确认的首个可用版本）。2026-09-20 20:54 +08:00 在该 HEAD、完整工作区无改动的状态执行 `npm test`，144/144 通过；`git diff --check` 和 `git diff --cached --check` 通过。基线至检查时 HEAD 的提交及文件 delta 均为空。
- 本次 closeout 仅补齐本文件的验证证据绑定，不改变运行代码、测试或依赖；上述测试证据因此继续适用于本次文档提交。README 当前使用说明与本台账为 current，旧版本段落为 history。本轮未读取或重新验证压缩包，包一致性记载是上一轮收束的历史核对结果，不作为本轮 HEAD 测试证据。
- 验证命令：`npm test`，144 项通过（阶段收束时复验）。
- 交付包：`PagePure-0.6.24.zip`；阶段收束时逐文件核对其与 `extension/` 一致。
- 无独立 build/lint 脚本，扩展直接加载 JavaScript；构建和 lint 不适用。
- 此轮不重新操作用户 Edge 或调用真实 Jev；自动化测试不能代替所有网站的端到端兼容验证。
- Git：经用户明确授权，在当前项目初始化本地仓库，以现有 0.6.24 源码和交付包为初始实物基线。首次提交包含源码、测试、演示、依赖配置及文档；不包含压缩包、依赖目录和临时输出。提交前运行 `git diff --cached --check`；准确提交标识由 Git 历史查询，不在本文件写入自身提交哈希。未推送远程。

## 0.6.24 商店发布（2026-09-20）

v0.6.24 已于 2026-09-20 提交 Edge 加载项商店审核（微软口径 7 个工作日内反馈，结果邮件发账户邮箱）。提交信息快照、商店文案与材料清单以 [STORE_PUBLISH.md](STORE_PUBLISH.md) 为准。

- Microsoft Store ID `0RDCKDS5H0H3`；CRX ID `ijiaoneehjiebocilhnbmkoohlleaeen`；产品 ID `647794d8-17b4-4ca3-954f-ecd211d3b985`。
- 商店发布包 `PagePure-0.6.24-store.zip`：与 `extension/` 逐文件一致；较历史包移除两张未引用的图标源图（内容相同，保留一份于 `assets/pagepure-icon-source.png`）并新增 `extension/icons/pagepure-256.png` 商店 logo；运行代码与 manifest 与历史包一致。
- 私有材料：隐私政策中英双语 gist；截图取自 `demo.html` 演示页。商店审核通过后需把商店链接回填 README 与 STORE_PUBLISH.md。

## 剩余事项与交接

下一位维护者先读 README 和本台账。后续优先按实际反馈验证不同页面、动态侧栏和网站改版后的稳定定位；模型仍可能误判，快照不能保证新结构全部立即显示。封闭 Shadow DOM、Canvas 内部及浏览器内部页面不支持。

保持已确认的块点选视觉和交互，不扩大 UI 改动范围。不清空已有规则、快照或密钥，不 reset/clean，不以旧包覆盖当前源码。Edge 商店提审已安排（见上节）；Git 远程推送仍未授权。
